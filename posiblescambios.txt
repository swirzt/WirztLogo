Hacer parser monádico con soporte de errores
hacer que el wait modifique el estado en vez de usar thread.delay
Cambiar ancho de dibujo (implica pensar como expandir cosas que estan en diagonal)
cambio de forma (dibujar formitas) (talVez bitmap)